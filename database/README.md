# Database Migrations

This directory is reserved for future database migration scripts.

Current database schema is available in:

```
database/schema.sql
```

Future releases of TeamFlow may integrate Flyway or Liquibase to maintain version-controlled database migrations.